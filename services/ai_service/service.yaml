apiVersion: v1
kind: Service
metadata:
  name: butler-identity-service-service
spec:
  selector:
    app: butler-identity-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: NodePort
